import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

//render 1st  = what to show, 2nd = where to show it
ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);

